# 设计说明：把众声合成为论断

> 「全体人民畅所欲言，超级计算机理解所有人的意图，最后合成为几个虚拟的意见领袖。」

这套系统把这个设想落到一个具体、可运行、可被质疑的工程上。本文说明**为什么这样设计**——
每一处取舍背后要防的是什么失败。

---

## 一、先说清楚不能怎么做

最直接的做法是：抓 3000 条推文，丢给大模型，让它"总结一下大家怎么看"。
这条路走不通，有三个各自独立的原因。

**1. 代表性会丢。** 大模型做摘要时报告的是**最生动**的观点，不是**最普遍**的观点。
一条写得漂亮的少数派发言，和五百条平淡的多数派发言放在一起，摘要出来往往是前者。
对于"想知道大多数人怎么想"这个目标，这是致命的——它系统性地偏向会写字的人。

**2. 结论不可追溯。** 合成出的论断如果不能落回具体的原帖，它就只是一段读起来合理的文字。
用来监控、进而影响仓位的东西，必须能一路点回去看是谁说的。

**3. 可被操纵。** 谁买了流量谁的话就是"大众意见"。刘慈欣设想里超级计算机的价值前提，
恰恰是它**不能被少数人买通**。

所以整套设计围绕一条原则展开：

> ### 模型不负责计数。
> 份额、账号数、权重、概率，全部由确定性的算术算出，然后作为**既定事实**交给模型。
> 模型唯一的工作是把这些数字写成人话。
> 代码位置：`pipeline/weighting.py` 算数，`llm/prompts.py` 写话，两者不重叠。

---

## 二、五层信源：为什么要分开，而不是揉成一个数

系统把 X 上的声音分成五层：**官方信息 / 专业媒体 / 英语区 KOL / 中文区 KOL / 大众用户**。

分层不是为了做加权平均。恰恰相反——

> **把五层揉成一个数，就毁掉了这个系统唯一真正值钱的东西。**

对一个监控者来说，"美联储加息概率 41%" 本身信息量有限（CME FedWatch 已经有了）。
真正有价值的是**层与层之间的落差**：

- 大众读数 65%、官方指引 12% → 市场在为一件官方没有暗示过的事情定价，
  要么是官方还没说，要么是大众在自己吓自己。两种都值得看一眼。
- 中文区 KOL 67%、英语区 KOL 51% → 同一件事，两个语言区在讲不同的故事。
- 专业媒体在**转述定价**，KOL 在**表达信念**——同样是 40%，含义完全不同。

所以 `Snapshot` 里每一层都保留独立读数，跨层的加权融合只是**附加**的一个数字，
并且它旁边永远列着分歧表。`pipeline/alerts.py` 里最高优先级的告警就是
`official_contradiction`：大众与官方指向相反。

各层的权重规则也不同，因为它们本来就是不同的仪器：

| 层级 | authority_weighting | engagement_weighting | 理由 |
|---|---|---|---|
| 官方信息 | 1.0 | 0.15 | 权威就是全部；三个赞的美联储声明仍然是美联储 |
| 专业媒体 | 0.85 | 0.5 | 机构信誉为主，传播度为辅 |
| 英/中文 KOL | 0.6 | 1.0 | 影响力真实存在，但不能压倒论证 |
| **大众用户** | **0.0** | 1.0 | **一人一票。40 个粉丝和 40000 个粉丝等重** |

大众层那个 `0.0` 是整套设计里最重要的一个常数。它就是"全体人民畅所欲言"这句话的实现。

---

## 三、从"言论"到"论断"：三步

### 第 1 步 立场抽取（每帖一次，可缓存）

每条帖子被独立读成一个结构化的立场：`stance` / `probability` / `key_claim` /
`reasoning_kind` / `intensity`。三个容易出错的地方被显式写进了 prompt：

- **这是谁的信念？** 路透说"期货定价 38%"不等于路透认为会加息。
  这类发言标为 `market_pricing`——数字照记（它就是信号），但不算作发言者的主张。
- **反讽与反问。** "他们肯定会降息呢，天天降 🙄" 的字面立场和真实立场相反。
- **数字不等于概率。** 一条宏观推文里的百分数，绝大多数是通胀率、利率、涨幅，
  **不是**事件概率。把 "unchanged at 4.25-4.50%" 读成 4.5% 的加息概率，
  会直接毁掉官方层的读数——这是开发过程中真实发生过的 bug，
  现在既写进了 prompt，也有一道兜底：`extract.py` 的一致性闸门会丢弃
  与立场锚点严重矛盾的数字（主张加息却给出 3% 概率，两者必有一错，
  而立场是更稳健的那个）。

`key_claim` 要求写成**去风格化的规范句**——去掉表情、称呼、修辞，只留主张和主要理由。
因为下一步要对它做聚类，保留下来的文风在那里就是噪声。

### 第 2 步 论点聚类（不是话题聚类）

聚类跑在 `key_claim` 上，不是原文上。原文会按语言和文风聚——"同意"和"agreed"
分得很远，而两段互不相干的火箭表情帖会聚在一起。

分区是两级的：
1. **按立场硬切**——多头和空头永远不是一个阵营，措辞再像也不行；
2. 立场内部按余弦相似度做**平均链接层次聚类**，阈值而非固定 k。
   一个议题上活跃的论点有几个，事先不知道；固定 k 只会把一个论点劈开或把两个焊死。

（`n > 400` 时降级为线性的 leader 聚类，代价是顺序依赖，收益是不卡住。）

### 第 3 步 合成虚拟意见领袖

每个达到份额门槛的簇，连同**算好的统计量**一起交给模型，产出一个 `Delegate`：

```
名字      数据派
论断      我们相信会加息。
理由      · 核心服务通胀连续三个月反弹
          · 工资增速仍在 4% 以上
反转条件  若核心 PCE 环比回到 0.2% 以下
份额      占该层 24% 的加权声量 · 6 条 / 6 个账号
引用      @user_zh_0 …  @user_en_5 …
```

给模型的约束写得很硬：**不得改动任何给定数字；只能说帖子里说过的话；
引用 id 必须来自给定集合**。最后一条在代码里还有一道校验
（`synthesize.py` 里对 `quote_ids` 取交集），因为约束写在 prompt 里不等于一定成立。

"宁可代表说得单薄，也不要它替不存在的人编理由"——这是 prompt 里的原话。

---

## 四、概率：两条独立估计，按覆盖率融合

对"加息概率"这类可量化议题，有两个互相独立的估计：

- `p_explicit`：明确给出数字的帖子的加权均值；
- `p_stance`：立场分布 × 立场锚点（`hike→0.90, hold→0.12, cut→0.02`）。

融合方式是 **按覆盖率加权**：

```
p = coverage · p_explicit + (1 − coverage) · p_stance
coverage = 给出了数字的那部分声量占比
```

很多人报数字时就信数字；没人报数字时才退回立场推断。
用任何其它方式混合，都会把同一个信念数两遍。

`unclear` 立场没有锚点，被**排除**在外而不是当成 0——这是另一个容易写错的地方。

---

## 五、抗操纵：四道闸

| 闸 | 机制 | 挡住什么 |
|---|---|---|
| 互动度对数封顶 | `min(1 + log₁₀(1+e)/2, 4.0)` | 一条 9 万赞的爆帖顶几条普通帖，不是几千条 |
| 重复内容衰减 | 同文本 n 份，每份记 `1/√n` | 复制粘贴刷屏：6 个号发同一句话 → 算 2.4 票 |
| 单账号上限 | 注水填充，任一账号 ≤ 该层 5% | 一个人连发 9 帖 → 从 28.7% 削到 5.0% |
| 新号/低粉惩罚 | 注册 <30 天 ×0.35；粉丝 <5 且未认证 ×0.4 | 批量小号 |

这些数字都在 `config.yaml` 里，不在代码里。demo 数据里埋了对应的攻击样本，
`tests/test_weighting.py` 和 `tests/test_pipeline.py` 会验证每一道闸真的咬合。

---

## 六、监控看的是变化，不是水位

`alerts.py` 比较相邻两个快照，只在五种情况下出声：

- `consensus_shift` 综合读数移动超过阈值
- `stance_flip` 某一层的主流立场换边
- `divergence` 两层之间的差距超过阈值
- `official_contradiction` 官方与大众指向相反（最高优先级）
- `new_argument` 出现了上一轮不存在、且已成规模的新论点

最后一条最容易被低估：**一个新论点的出现，往往比读数变化早**。
"因为财政部发债节奏"这种理由第一次出现在 KOL 层时，读数可能还没动。

---

## 七、成本

抽取是唯一烧 token 的环节，三层防护：

1. **按 (帖子, 议题) 落库**，并以 prompt 版本为键。重跑只为**新帖**付费。
2. **固定的评分规则作为缓存 system block**发送，变动的帖子放在断点之后。
3. **磁盘缓存**按 (模型, prompt 版本, 内容) 哈希。崩溃重跑、重新渲染报告，都不花钱。

一次 500 帖的完整循环，抽取约 20 次调用（batch=20）。`extract_effort: low` 是有意的——
抽取是机械活，不值得高 effort；合成用 `high`，因为那里的语言质量直接就是产品。

每次运行结束会打印实际 token 与估算成本。

---

## 八、已知局限（不加粉饰）

- **X API 是主要成本与瓶颈。** recent search 只覆盖最近 7 天，且读取额度按付费档位计。
  系统用 `since_id` 游标做增量拉取、默认排除转推，就是为了省这个额度；
  但"抓到全体人民"在 API 层面是做不到的，抓到的是**查询条件下的样本**。
  查询写得偏，结论就偏——这是最大的未建模误差，比模型误差大得多。
- **大众层的样本偏差无法靠权重修掉。** 一人一票只在"进入样本的人"里成立。
  会发英文推讨论 FOMC 的人，不是随机的人。
- **锚点是人设的先验。** `hike→0.90` 是个判断，不是测量。它影响没人报数字时的读数。
- **离线模式的语言是模板。** `--offline` 下统计全是真的，句子是套出来的。
  它的用途是验证管线和省钱，不是产出。
- **立场轴是预设的。** 系统能发现"新论点"，但发现不了"这个议题本身问错了"。
- **中文分词是字级 n-gram。** 够用，但比不上正经分词；换 `embeddings.provider: voyage`
  会明显改善跨语言的论点归并。

---

## 九、代码地图

```
sources/          X API v2 与 fixture 回放，同一个接口
pipeline/
  ingest.py       抓取，按查询打上层级先验
  cohort.py       白名单 → 查询先验 → 启发式 → 模型，由便宜到贵
  extract.py      立场抽取（批量、并发、幂等、可降级）
  weighting.py    ★ 全部算术都在这里，也就是这套系统的"政治"
  cluster.py      立场硬切 + 论点层次聚类
  synthesize.py   ★ 代表合成、层级论断、跨层分歧
  alerts.py       两个快照之差 → 信号
llm/
  prompts.py      ★ 三套 prompt 及其约束
  offline.py      无网络的确定性替身
  client.py       结构化输出、prompt 缓存、磁盘缓存、拒答降级
render/           终端与自包含 HTML 报告
```

三个 ★ 是这套设计的实质，其余是把它跑起来的管道。
